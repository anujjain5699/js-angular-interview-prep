# 🚀 JavaScript & Angular Interview Preparation

A curated collection of JavaScript machine coding challenges, asynchronous programming patterns, closures, and Angular core concepts with in-depth explanations and mental models.

---

## 📚 Topics & Machine Coding Solutions

<!-- AUTO-GENERATED-INDEX:START -->
<!-- This section is automatically generated by running: node update_readme.js -->

| # | Topic / Problem | File Link | Category | Key Concept |
|---|---|---|---|---|
| 1 | **Angular Lifecycle Hooks** | [`angular hooks`](./angular%20hooks) | Angular Core | Component lifecycle, Change Detection |
| 2 | **Fixed Currying** | [`currying.js`](./currying.js) | Functional JS | Closure memory, recursion, rest operator |
| 3 | **Debounce & Throttle** | [`debounce_throttle.js`](./debounce_throttle.js) | Performance & Timing | `clearTimeout`, `isWaiting` cooldown flag |
| 4 | **Infinite Currying** | [`infiniteCurry.js`](./infiniteCurry.js) | Functional JS | `()` empty call terminator, closures |
| 5 | **Parallel Execution Limit** | [`parallel_limit.js`](./parallel_limit.js) | Async / Concurrency | Worker Pool pattern, `Promise.all` |

<!-- AUTO-GENERATED-INDEX:END -->

---

## 🧠 Core Mental Models & Cheatsheets

### 1. Debounce vs. Throttle
* **Debounce (Elevator Door 🚪)**: *"Wait until the user completely stops for `delay` ms before executing."*
  * **Rule**: `clearTimeout(timer)` + `setTimeout(...)`
  * **Use Case**: Search bar auto-complete, window resize end.
* **Throttle (Weapon Cooldown 🔫)**: *"Execute immediately on 1st trigger, then ignore all calls for `delay` ms."*
  * **Rule**: `if (isWaiting) return;` + `isWaiting = true;` + `setTimeout(() => isWaiting = false, delay)`
  * **Use Case**: Scroll listeners, button double-click spam prevention.

### 2. Worker Pool Pattern (Parallel Async Limit)
* Instead of manual recursion, launch `limit` independent `async` workers running `while (currentIndex < tasks.length)` loops.
* `Promise.all(workers)` coordinates all workers and resolves when all tasks finish.

### 3. Infinite Currying
* Accumulate arguments using `...args` and return an inner function `(...nextArgs)`.
* Stop condition: `nextArgs.length === 0` $\rightarrow$ reduce and return total.

---

## ⚡ How to Auto-Update this README

Whenever you add a new `.js` file or modify code, simply run:

```bash
node update_readme.js
```

This will automatically scan your folder and update the table of contents above!

---

## 🛠️ Running Solutions Locally

Make sure you have [Node.js](https://nodejs.org/) installed:

```bash
# Run Parallel Limit demo
node parallel_limit.js

# Run Currying demo
node currying.js

# Run Infinite Currying demo
node infiniteCurry.js

# Run Debounce & Throttle demo
node debounce_throttle.js
```

---

⭐ Maintained by [Anuj Jain](https://github.com/anujjain5699)
